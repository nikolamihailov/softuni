function cityRecord(cityName, cityPopulation, cityTreasury) {
    const cityRecord = {
        name: cityName,
        population: cityPopulation,
        treasury: cityTreasury
    };
    return cityRecord;
}