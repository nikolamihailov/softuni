<main class="create">
  <!-- Create Page (Only for logged-in users) -->
  <section id="create">
    <div class="form">
      <h2>Add item</h2>
      <form class="create-form" method="POST">
        <input
          type="text"
          name="name"
          id="name"
          placeholder="Name"
          value="{{animalData.name}}"
        />
        <input
          type="text"
          name="years"
          id="years"
          placeholder="Years"
          value="{{animalData.years}}"
        />
        <input
          type="text"
          name="kind"
          id="kind"
          placeholder="Kind"
          value="{{animalData.kind}}"
        />
        <input
          type="text"
          name="image"
          id="animal-img"
          placeholder="Image url"
          value="{{animalData.image}}"
        />
          <img id="animalImage"src="{{#if animal.image}}{{animal.image}}{{else}}/images/no-image.jpg{{/if}}" alt="{{animal.name}}" style="width: 80px; height: 80px; margin-bottom:10px">
        <input
          type="text"
          name="need"
          id="needs-of"
          placeholder="Needs of"
          value="{{animalData.need}}"
        />
        <input
          type="text"
          name="location"
          id="location"
          placeholder="Location"
          value="{{animalData.location}}"
        />
        <textarea
          id="description"
          placeholder="Description..."
          name="description"
        >{{animalData.description}}</textarea>
        <button type="submit">Post</button>
      </form>
    </div>
  </section>
</main>

<script>
  const imageUrlInput = document.getElementById('animal-img');
  const animalImg = document.getElementById('animalImage');
  imageUrlInput.addEventListener('input', function () {
  const imageUrl = imageUrlInput.value;
   animalImg.src =imageUrl;   
   });
</script>